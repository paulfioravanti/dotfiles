" ------------------------------------------------------------------------------
" vim-airline and related config
" ------------------------------------------------------------------------------
" Suppress default mode messages and just use vim-airline
set noshowmode
" TODO: Set status bar to something better than it currently is.
" Powerline fonts meant that collapsing statuses didn't work and info was
" obscured, so it's been removed, but it still looks ugly.
